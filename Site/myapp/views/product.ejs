<!DOCTYPE html>
<head lang="es">
	<%- include('partialls/head') %> 
</head>
<body>
	<%- include('partialls/header') %>
	<main>
            <section class="products" style="display: flex; flex-wrap: wrap; flex-direction: row; justify-content: space-between;">
                <% for(let i = 0; i < products.length; i++) { %> 
                    <%- include('partialls/product', {products: products[i]}) %>
                <% } %>
            </section>
        </main>
        <%- include('partialls/footer') %>
    </body>
</html>